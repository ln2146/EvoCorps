# EvoCorps 前端启动指南

## 🚀 快速启动（推荐）

### 方法 1：使用启动脚本（一键启动）

**Windows 用户：**
```bash
cd frontend
start.bat
```
或者直接双击 `frontend/start.bat` 文件

**命令行启动：**
```bash
cd frontend
npm start
```

这个命令会自动：
1. ✅ 启动后端 API 服务（端口 5000）
2. ✅ 启动前端开发服务器（端口 3000）
3. ✅ 自动打开浏览器

### 方法 2：手动分别启动

如果需要分别启动（用于调试）：

**终端 1 - 启动后端 API：**
```bash
# 在项目根目录
python frontend_api.py
```

**终端 2 - 启动前端：**
```bash
cd frontend
npm run dev
```

## 📦 首次使用

首次使用需要安装依赖：

```bash
# 安装 Python 依赖
pip install flask flask-cors

# 安装前端依赖
cd frontend
npm install
```

## 🌐 访问应用

启动成功后，浏览器会自动打开：`http://localhost:3000`

## 📊 数据库配置

- 数据库文件应放在项目根目录的 `database/` 文件夹
- 支持的格式：`*.db` (SQLite)
- 前端会自动扫描并在下拉框中显示所有可用数据库

## 🔧 故障排查

### 1. 端口被占用
```bash
# 检查端口占用
netstat -ano | findstr :5000
netstat -ano | findstr :3000

# 结束占用进程
taskkill /PID <进程ID> /F
```

### 2. 后端 API 无法连接
- 确保 Python 和 pip 已安装
- 确保 flask 和 flask-cors 已安装
- 检查 `database/` 文件夹是否存在

### 3. 前端无法启动
- 确保 Node.js 已安装（建议 v18+）
- 删除 `node_modules` 和 `package-lock.json`，重新运行 `npm install`

## 📝 可用命令

```bash
npm start       # 同时启动后端和前端（推荐）
npm run dev     # 仅启动前端
npm run build   # 构建生产版本
npm run preview # 预览生产版本
```

## 🛑 停止服务

按 `Ctrl + C` 停止所有服务
